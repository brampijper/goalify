html
	head
		include includes/header.pug
	body
		div.container
			if (userinfo === null)
				div.row
					div.col.s12
						h2 Oops!
				div.row.rowbackgr
					div.col.s12
						p 
							| It seems page you are looking for can not be found. Do you perhaps wish to 
							a(href='leaderboard') view the leaderboard
							| , 
							a(href='profile') edit your own profile 
							| or 
							a(href='goaloverview') view all goals
							| ?

						h3 We're really sorry about this.
						if (currentUser.kindOfPerson == 'cat')
							p Since you like cats so much, here is a picture of a sad cat:
							img(src='images/sadcat.png'  class='circle profifo')
						else
							p Since you like dogs so much, here is a picture of a sad dog:
							img(src='images/saddog.jpg'  class='circle profifo')


							//- This part is only ever visible when the user is logged in, therefore the option login or register is unnecessary

			else
				div.row
					div.col.s12
						h2 #{userinfo.username}'s profile
						h4= message

				div.row.rowbackgr
					div.col.s12.m6.l6
						h4 #{userinfo.username}
						h5 "#{userinfo.bio}"
						p Age: (hier nog datumnotatie omzetten) #{userinfo.dob}
						if userinfo.id === currentUser.id
							div.highlighted
								p
									i.medium.material-icons.bulbleft lightbulb_outline
									| Is this information incorrect? 
									a(href='profile') Click here 
									| to change your personal details.

					div.col.s12.m6.l6
						if (userinfo.kindOfPerson == 'cat' && userinfo.profifo.length === 0)
							img(src='images/catperson.jpg' class='circle profifo')

						else if (userinfo.kindOfPerson == 'dog' && userinfo.profifo.length === 0)
							img(src='images/dogperson.jpg' class='circle profifo')

						else 
							img(src=userinfo.profifo, class='circle profifo')
				div.row
					div.col.s12
						h4 Game achievements:
				div.row.rowbackgr
					div.col.s12.m6.l6
						p 
							strong Playing for team: 
							| #{userinfo.kindOfPerson}
						p 
							strong Current score: 
							| #{userinfo.score}
					div.col.s12.m6.l6
						p Accomplished goals (plus datum?? of Last completed goal (dus maar 1, plus datum)

		main
		include includes/footer.pug


